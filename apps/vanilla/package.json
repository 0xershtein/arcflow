{
	"name": "vanilla-example",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"description": "Uses the built @arcflow/editor bundle with no framework. Run `pnpm build` at the root first.",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@arcflow/core": "workspace:*",
		"@arcflow/editor": "workspace:*",
		"@arcflow/payments": "workspace:*"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	}
}
