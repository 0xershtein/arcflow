# Surface: docs home (the landing)

Route: `apps/docs/src/routes/+page.svelte` · Mode: Persuade · Audience: developers and coding agents arriving from GitHub.

## Direction contract

THESIS: One step definition, and the four things it yields, arranged as the page itself. It refuses the feature-grid landing where equal cards of icon-plus-heading-plus-text stand in for evidence, and it refuses the screenshot hero: the editor runs on the page instead of posing in an image.

OWN-WORLD: The docs site's committed system, inherited unchanged — white / near-black ground, one indigo accent (#4f46e5 light, #818cf8 dark), hairline rules at #e4e4e7 / #27272a, system sans for prose, mono for every artifact. Recognizable with the content removed by its ruled bands, its one accent used only for the live and the wrong, and panels that hold real code rather than decoration. Browser surfaces — selection, caret, scrollbars, focus rings, tabular numerals — themed from the same palette.

STORY: The visitor understands in one line that this is a flow engine with an embeddable canvas; believes it because the definition, the validation issue, the catalog entry and the running editor are all the real thing on the page; and acts by copying the install command or opening the quick start. A model reading the page finds the catalog, the MCP command and the issue format in plain text.

FIRST VIEWPORT: Left, at 7 of 12 columns: the headline at display scale, one sentence of deck, the install command in a copyable block, then Quick start (filled, accent) beside GitHub (hairline). Right, at 5 columns and quieter: the honest status — 0.1.0, not on npm yet, the clone that does work today, MIT. Directly below the fold line, centred and unhurried, one real `defineNode` at 8 of 12 columns; the four output panels follow it in one row, each holding its own artifact.

FORM: Structure 4 of my ordered list of seven, dealt by the roll and locked by the user against "the spec sheet" and "the session an agent ran". Seed key 60ef4942, surface scope, persuade.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
